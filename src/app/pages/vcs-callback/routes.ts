import { VcsCallbackComponent } from './vcs-callback.component';
import { Routes } from '@angular/router';

export const routes: Routes = [
  {
    path: '',
    component: VcsCallbackComponent
  }
];
